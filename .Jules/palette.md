# Palette's Journal

## 2025-05-14 - [Aria Attribute Learning]
**Learning:** `aria-required` is an invalid attribute for `button` elements. It should only be used on input-related elements like `input`, `select`, `textarea`, or elements with roles that support it.
**Action:** Always verify if an ARIA attribute is supported by the specific HTML element or role before applying it.
